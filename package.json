{
  "name": "predicate-workbench",
  "private": true,
  "type": "module",
  "description": "Predicate investigation workbench. No runtime dependencies; see docs/APPLICATION-DESIGN.md.",
  "scripts": {
    "build": "python3 build/build.py",
    "test": "node --test \"tests/*.test.js\"",
    "test:e2e": "python3 build/build.py && node --test --test-concurrency=1 \"tests/e2e/*.test.js\""
  }
}
